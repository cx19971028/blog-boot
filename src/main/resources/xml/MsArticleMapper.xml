<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njtech.blog.mapper.MsArticleMapper">
    <select id="listArchives" resultType="com.njtech.blog.dto.Archives">
        select FROM_UNIXTIME(create_date/1000,'%Y') as year, FROM_UNIXTIME(create_date/1000,'%m') as month,count(*) as count from ms_article group by year,month
    </select>

    <select id="listArticle" resultType="com.njtech.blog.entity.MsArticle">
        select * from ms_article
        <where>
            1=1
            <if test="categoryId!=null and categoryId.length>0">
                and category_id=#{categoryId}
            </if>
            <if test="tagId!=null and tagId.length>0">
                and id in (select article_id from ms_article_tag where tag_id=#{tagId})
            </if>
            <if test="year!=null and year.length >0 and month!=null and month.length>0">
                and (FROM_UNIXTIME(create_date/1000,'%Y')=#{year} and FROM_UNIXTIME(create_date/1000,'%m')=#{month})
            </if>
        </where>
            order by create_date desc
    </select>
</mapper>
