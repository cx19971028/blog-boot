<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.njtech.blog.mapper.MsTagMapper">

    <select id="findTagsByArticleId" parameterType="String" resultType="com.njtech.blog.entity.MsTag">
        select id,avatar,tag_name as tagName from ms_tag
        where id in (select tag_id from ms_article_tag where article_id = #{id})
    </select>

    <select id="selectHotTagIds" parameterType="int" resultType="java.lang.Long">
        SELECT tag_id from ms_article_tag group by tag_id ORDER BY count(*) desc limit #{limit}
    </select>
</mapper>
